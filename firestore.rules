rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all collections for the institutional leave portal
    // In production, granular ABAC rules can be layered on top of role checks
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
