// Firebase disconnected permanently per project specification.
// All database operations are directed exclusively to PostgreSQL (Neon DB).
export const app = null as any;
export const db = null as any;

